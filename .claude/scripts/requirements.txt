# Webnovel Writer - Python Dependencies
# Python >= 3.10 required

# Runtime dependencies
aiohttp>=3.8.0,<4.0.0          # 异步 HTTP 客户端（API 调用）
filelock>=3.0.0,<4.0.0         # 文件锁（状态文件并发控制）
pydantic>=2.0.0,<3.0.0         # Schema 校验

# Dev/Test dependencies
pytest>=7.0.0,<9.0.0           # 单元测试
pytest-cov>=4.1.0,<6.0.0       # 覆盖率统计
pytest-asyncio>=0.23.0,<1.0.0  # 异步测试支持
pytest-timeout>=2.3.0,<3.0.0   # 测试超时保护
